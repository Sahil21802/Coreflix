{
  "installCommand": "npm install --force",
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "server/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "api/(.*)",
      "dest": "server/index.js"
    }
  ]
}
